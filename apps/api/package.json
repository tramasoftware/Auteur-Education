{
  "name": "@auteur/api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn auteur_api.main:app --reload --host 127.0.0.1 --port 8000",
    "lint": "uv run ruff check src tests",
    "test": "uv run pytest"
  }
}
