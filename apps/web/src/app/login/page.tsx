import { PlaceholderPage } from "@/components/layout";

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Sign in"
      description="Authentication happens after a proposal is selected and before checkout."
    />
  );
}
