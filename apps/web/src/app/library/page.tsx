import { PlaceholderPage } from "@/components/layout";
import { LibraryFeature } from "@/features/library";

export default function LibraryPage() {
  return (
    <PlaceholderPage
      title="Library"
      description="Personal collection of courses, confirmed progress, and resumed study."
    >
      <LibraryFeature />
    </PlaceholderPage>
  );
}
