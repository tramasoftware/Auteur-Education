import { PlaceholderPage } from "@/components/layout";
import { ProposalsFeature } from "@/features/proposals";

export default function ProposalsPage() {
  return (
    <PlaceholderPage
      title="Course proposals"
      description="Present genuinely different learning directions after the objective is confirmed."
    >
      <ProposalsFeature />
    </PlaceholderPage>
  );
}
